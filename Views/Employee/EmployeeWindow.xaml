<Window x:Class="Store_Management.Views.Employee.EmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Store_Management.Views.Employee"
        xmlns:employeeVM="clr-namespace:Store_Management.ViewModels.EmployeeVM"
        d:DataContext="{d:DesignInstance employeeVM:EmployeeWindowVM}"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="EmployeeWindow" Height="400" Width="500"
         Style="{StaticResource MaterialDesignWindow}"
        >

    <Grid Background="#f0f0f0">

        <ContentControl Content="{Binding CurrentPage}" />
        
    </Grid>
</Window>
